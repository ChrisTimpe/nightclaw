# NightClaw — Install

Install instructions, phased install table, placeholder reference, cron setup, heartbeat guidance, and validation steps are in **[README.md § Install](README.md#install)**.

> This file is required by `scripts/validate.sh` and `scripts/install.sh`. Do not delete it.
