# NightClaw — Install

This file is a router. The full install walkthrough lives in
**[README.md § Install](README.md#install)** — operator path from OpenClaw
onward, post-install checklist, and validation steps in order.

After install, continue with **[DEPLOY.md](DEPLOY.md)** for cron creation,
model configuration, heartbeat tuning, and uninstall procedure.

> This file is required by `scripts/validate.sh` and `scripts/install.sh`. Do not delete it.
